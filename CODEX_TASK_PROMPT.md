# 给 Codex 的开发任务提示词

你是前端工程师，请开发一个单页 HTML 互动网页游戏：《火星探索·中国号》。这个网页用于家长会/课堂分享，面向小学四年级学生，用火星探索故事讲解软件开发中的四个岗位：产品岗、设计岗、开发岗、维护岗。

请先完整阅读并遵守这些文件：

1. `PRD_v2.0.md`
2. `CONTENT_COPY.md`
3. `GAME_CONFIG_REFERENCE.js`
4. `VISUAL_ASSETS_GUIDE.md`
5. `ACCEPTANCE_CHECKLIST.md`

## 最终交付

请生成一个文件：

```txt
index.html
```

要求：

- 单 HTML 文件。
- CSS 和 JavaScript 全部内联。
- 不需要后端。
- 不需要构建工具。
- 不引入外部图片、音频、字体或复杂依赖。
- 可以使用 Emoji、CSS、SVG 或简单 HTML 结构绘制飞船、地球、火星、小行星等元素。
- 页面可直接用浏览器打开运行。
- 主要适配电脑浏览器投屏。

## 核心功能

实现五个页面/视图：

1. 首页大厅
2. 产品岗：任务队长
3. 设计岗：飞船造型师
4. 开发岗：代码驾驶员
5. 维护岗：故障救援员
6. 最终总结页

这些视图都在同一个 `index.html` 中，通过 JavaScript 控制显示/隐藏，不要使用多页面跳转。

## 顶部导航

页面顶部固定或半固定显示四个关卡流程：

- 🚀 任务队长：想清楚
- 🎨 飞船造型师：画出来
- 💻 代码驾驶员：做出来
- 🛠️ 故障救援员：守护它

要求：

- 当前关卡高亮。
- 已完成关卡显示 ✅。
- 点击任意关卡可跳转。
- 首页和最终总结页也应能看到这个流程区，或至少有清楚的返回首页入口。

## 每关统一要求

每个关卡必须包含：

- 关卡标题。
- 简短任务说明。
- 互动游戏区域。
- “💬 讲解提示”按钮。
- 完成后显示岗位总结卡片。
- “上一关 / 下一关 / 返回首页”一类的导航按钮。

讲解提示点击后弹出 modal 或浮层，显示 `CONTENT_COPY.md` 中对应的主持人讲解词。

## 产品岗关卡要求

实现：

- 显示总目标：“把航天员安全送到火星，完成科学探索，再平安回家。”
- 显示幽灵飞船。
- 显示三个愿望泡泡：最强防护罩、最多科学仪器、极速往返。
- 点击泡泡后显示好处和代价。
- 点击“最终决定”后显示任务单。
- 幽灵飞船变为确定形态，例如出现防护盾、探测臂等视觉变化。
- 显示产品岗总结卡片。
- 标记产品岗完成。

## 设计岗关卡要求

实现：

- 显示一个可分区域涂色的飞船。
- 至少 4 个区域：机身主体、防护盾条纹、探测臂、尾翼。
- 调色板包含：红、金、蓝、银、白。
- 点击区域后选择颜色可涂色。
- 区域可以反复改色。
- 有“🇨🇳 国旗标志”按钮。
- 点击国旗按钮后，提示选择放置位置。
- 国旗可放置到：机身左侧、机身右侧、尾翼、防护盾旁边。
- 国旗只能有一个；再次选择位置时移动国旗。
- 点击“完成设计”后播放飞船展示动画。
- 显示设计岗总结卡片。
- 标记设计岗完成。

## 开发岗关卡要求

这是最关键的互动之一，请重点实现。

实现：

- 显示网格迷宫。
- 支持设置网格大小：5×5、6×6、7×7。
- 支持设置难度：简单、普通、挑战。
- 更改设置后点击“重新生成地图”。
- 起点在左下角。
- 终点在右上角。
- 飞船初始朝向向上。
- 障碍物是小行星。
- 使用 `GAME_CONFIG_REFERENCE.js` 中的迷宫预设或自行生成，但必须保证有路可达。

代码控制区：

- 三个按钮：
  - ⬆️ 前进，生成 `forward()`
  - ↩️ 左转90°，生成 `turnLeft()`
  - ↪️ 右转90°，生成 `turnRight()`
- 点击按钮后，代码框新增一行代码。
- 点击“▶️ 执行代码”后：
  - 从第一行开始逐行高亮。
  - 每高亮一行，飞船执行对应动作。
  - `forward()` 按当前朝向前进一步。
  - `turnLeft()` 原地左转 90°。
  - `turnRight()` 原地右转 90°。
  - 飞船图标应根据方向旋转。
  - 每步间隔约 500ms。
- 执行中禁用指令按钮。
- 撞到小行星或出界：
  - 停止执行。
  - 飞船抖动。
  - 显示：“碰到石头了！代码还需要再想想。”
  - 飞船回到起点。
  - 可以清空代码，降低课堂复杂度。
- 到达终点：
  - 显示：“到达火星！代码成功！”
  - 播放庆祝动画。
  - 显示开发岗总结卡片。
  - 标记开发岗完成。

## 维护岗关卡要求

实现：

- 飞船返航场景。
- 远处地球。
- 状态条。
- 当前危机提示。
- 四个应急按钮：
  - 🔥 加温
  - ❄️ 降温
  - ☀️ 展开太阳帆板充电
  - 🧑‍🚀 太空行走维修
- 支持设置：
  - 成功处理危机次数：3、5、6，默认 3。
  - 危机出现速度：快速、普通、慢速，默认快速。
- 点击“开始返航任务”后触发危机。
- 危机不要连续重复同一种。
- 每种危机有不同视觉效果：结霜、发红、变暗、裂痕火花。
- 每次危机有倒计时条，默认约 5 秒。
- 点对：解除危机、状态回升、成功次数 +1。
- 点错：状态下降，提示“不对哦，再想想！”。
- 超时：状态下降，但仍允许继续选择直到选对。
- 状态降到最低时不要直接游戏失败，避免课堂中断，可以显示鼓励提示。
- 达到目标次数后：
  - 停止危机。
  - 播放安全回到地球动画。
  - 显示：“守护成功！火星样本安全抵达地球！”
  - 显示维护岗总结卡片。
  - 标记维护岗完成。

## 最终总结页

四关都完成后，显示“查看总结”按钮，或自动进入最终总结页。

总结页包含：

- 标题：“🎉 恭喜完成火星探索任务！”
- 火星任务与软件开发对照表。
- 金句：“一个好软件，不是一个人变出来的，而是一支团队一起完成的。”
- “再玩一次”和“返回首页”按钮。

## 视觉要求

- 儿童友好。
- 太空卡通风。
- 深空蓝紫渐变背景。
- 圆角卡片。
- 大字号。
- 大按钮。
- 强反馈动画。
- 适合投屏。
- 不要做得太小、太密、太复杂。

## 代码质量要求

请使用清晰的结构：

- 全局状态对象 `appState`。
- 关卡切换函数。
- 每个关卡独立的初始化/重置函数。
- 数据配置尽量集中管理。
- 定时器要妥善清理，尤其维护岗危机定时器和倒计时。
- 避免重复绑定事件导致多次触发。
- 执行开发岗代码时，按钮禁用，执行结束后恢复。

## 手动测试要求

开发完成后，请根据 `ACCEPTANCE_CHECKLIST.md` 逐项检查。尤其确认：

- 首页可以进入四关。
- 顶部导航当前状态正确。
- 每关讲解提示可以打开和关闭。
- 每关完成后显示总结卡片。
- 开发岗代码逐行高亮，飞船同步移动。
- 维护岗点错、超时、点对都有反馈。
- 四关完成后能进入最终总结页。

